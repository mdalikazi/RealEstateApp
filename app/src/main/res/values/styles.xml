<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
        <item name="android:textColorTertiary">@color/colorTextPrimary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.ToolbarPlaceholder">
        <item name="android:windowBackground">@drawable/large_toolbar</item>
    </style>

    <style name="SentiaEmptyMessageStyle">
        <item name="android:layout_centerInParent">true</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">?android:textColorSecondary</item>
        <item name="android:visibility">gone</item>
    </style>

    <style name="SentiaBusinessIconStyle">
        <item name="android:foreground">@drawable/image_circular_border</item>
        <item name="android:contentDescription">@string/image_content_description_default</item>
    </style>

    <style name="SentiaTextHeaderStyle" parent="@android:style/TextAppearance.Large">
    </style>

    <style name="SentiaTextSecondaryHeaderStyle" parent="@android:style/TextAppearance.Large">
        <item name="android:textStyle">bold|italic</item>
    </style>

    <style name="SentiaSecondaryText">
        <item name="android:textColor">?android:textColorSecondary</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
